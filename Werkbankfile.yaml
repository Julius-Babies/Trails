project:
  name: "Trails"
  id: "trails"
  external_domain: "trailsdevelopment.jvbabi.es"

services:
  - name: "api"
    path_prefixes:
      - "/api"
    modes:
      local:
        port: 8080

  - name: "frontend"
    path_prefixes:
      - "/"
    modes:
      local:
        port: 5173

dependencies:
  androiddns: true
